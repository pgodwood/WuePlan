# WuePlan

# Backend

## Dependencies

- python
- pip

install virtual environment:
`$ python -m venv venv`

activate virtual envirionemnt. to deactivate, just `(venv)$ deactivate`
> $ cd venv/scripts
$ activate
